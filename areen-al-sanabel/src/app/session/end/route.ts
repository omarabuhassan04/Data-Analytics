import { redirect } from "next/navigation";

import { clearSessionCookie } from "@/lib/auth";

/**
 * إنهاء جلسة لم تعد صالحة.
 *
 * الحالة: التوكن سليم التوقيع لكن صاحبه لم يعد موجودًا أو عُطّل حسابه
 * (مثلًا بعد حذف الحساب أو إعادة تهيئة قاعدة البيانات). في هذه الحالة
 * لا تستطيع الطبقة الوسيطة (middleware) اكتشاف المشكلة لأنها لا تصل إلى
 * قاعدة البيانات، فتسمح بالمرور، بينما ترفض صفحات التطبيق العرض —
 * ما يسبّب حلقة إعادة توجيه لا تنتهي.
 *
 * الحل: توجيه هذه الحالة إلى هنا، حيث يمكن حذف الكوكي فعليًا
 * (وهو ما لا يجوز فعله أثناء عرض مكوّن خادم) ثم الانتقال إلى صفحة الدخول.
 */
export async function GET() {
  await clearSessionCookie();
  redirect("/login");
}
